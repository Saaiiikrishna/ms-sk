# Simple Dockerfile using pre-built JAR
FROM openjdk:17-jre-alpine

# Set working directory
WORKDIR /app

# Copy the pre-built JAR
COPY target/zookeeper-service-1.0.0.jar app.jar

# Expose port
EXPOSE 8084

# Run the application
ENTRYPOINT ["java", "-jar", "app.jar"]
